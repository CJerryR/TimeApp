# 更新日志

本文件依据当前 Git 历史、像素桌宠实施报告、液态玻璃重做报告与有效 smoke 结果整理。未在提交历史或验证证据中出现的版本号、日期和功能不在此补写。

## 未发布（当前开发工作树）

### 桌面应用重构

- 将活动应用从旧的单 HTML 形态迁移为 Electron、React、TypeScript、Vite 与 sql.js 组成的 Windows 桌面应用。
- 建立主进程、preload、renderer、shared 类型和本地 SQLite 数据层。
- 提供当前活动、对话、规划、记忆、接入、设置六个主页面，以及独立的内部 Design Lab。
- 增加系统托盘、单实例、开机自启动、JSON 导入导出、主窗口与桌宠窗口协同。

### 2D 像素桌宠迁移

- 将活动桌宠实现统一迁移到 `pixel-sprite`，接入“若涵 · 像素桌宠”模型。
- 提供 3 种位置与 8 种状态，共 24 张 48×64 PNG 帧，并使用像素化渲染。
- 完成点击、拖动、双击、任务栏停靠、窗口座位、自由位置、边缘吸附、缩放、置顶、隐藏和鼠标穿透恢复。
- 从活动源码与构建产物中移除 Live2D、Layered2D 和旧角色运行时/资源；迁移前材料与回退步骤保留在活动仓库之外的工作区归档中。
- 明确迁移归档不包含用户 SQLite 数据，源码回退前必须单独备份用户数据。

### 六页液态玻璃重做

- 统一重做当前、对话、规划、记忆、接入、设置六个主页面的液态玻璃视觉与组件语义。
- 集中设计令牌、浅色/深色主题、半透明玻璃表面、实色 fallback、语义色、阴影、描边和焦点环。
- 移除首页右半区域贯穿全高的硬分割，使首页恢复连续舞台背景。
- 收口规划页日/周/月/年日历、记忆页单主操作、设置页单层工作区和外部线索确认队列。

### 交互、动画与响应式

- 集中管理 90–260ms 的交互节奏，页面和日历空间过渡不超过 280ms。
- 增加应用内减弱动效、系统 `prefers-reduced-motion` 和降低透明度支持。
- 增加 `Ctrl+1` 至 `Ctrl+6` 页面快捷导航、焦点转移、跳过导航和 ARIA 契约。
- 验证 1180×780 桌面视口与 420×900 窄视口，覆盖浅色、深色、正常动效、减弱动效、七列月历和横向溢出。

### 验证

- 有效批次 `2026-07-10T23-07-33-106Z` 通过 typecheck、build 与 Electron smoke。
- Legacy contract `104/104`、Visual `22/22`、总计 `127/127`，无失败断言。
- 验证活动源码与构建产物中的 Live2D / Layered2D 违规数为 0。
- 记录透明桌宠窗口截图的合成边界：smoke 截图不能证明生产窗口的桌面 alpha 合成。

## 2026-07-11 — 接入若涵像素桌宠设置 UI

对应提交：`4880034`（`feat: 接入若涵像素桌宠设置 UI`）。

- 新增像素桌宠设置预览组件。
- 在首页与设置页接入若涵像素桌宠相关界面。
- 该提交只覆盖当时纳入 Git 的 3 个 renderer 文件，不代表上述未发布桌面重构已全部提交。

## 2026-06-13 — TimeMate v8.0 赛道纪律版

对应提交：`af879e9`（`feat: TimeMate v8.0 赛道纪律版`）。

- 建立 TimeMate v8.0“赛道纪律版”的单 HTML 应用基线。
- 该版本是当前 Electron 桌面重构之前的历史形态；其提交记录仍保留在仓库历史中。
